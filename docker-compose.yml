version: '3.8'

services:
  postgres_db:
    image: postgres:15
    container_name: mi_base_de_datos
    environment:
      - POSTGRES_USER=usuario_etl
      - POSTGRES_PASSWORD=password123
      - POSTGRES_DB=clima_db
    ports:
      - "5433:5432" 